require 'spec_helper'

describe TimeAlert do
  it 'has a version number' do
    expect(TimeAlert::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
